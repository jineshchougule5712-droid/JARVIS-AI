services:
  postgres:
    image: postgres:16
    environment:
      POSTGRES_DB: jarvis
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports: ["5432:5432"]
    volumes: [jarvis_data:/var/lib/postgresql/data]
volumes:
  jarvis_data:
