services:
  - type: web
    name: jarvis-ai
    runtime: docker
    plan: free
    dockerfilePath: ./Dockerfile
    envVars:
      - key: DB_HOST
        fromDatabase:
          name: jarvis-db
          property: host
      - key: DB_PORT
        fromDatabase:
          name: jarvis-db
          property: port
      - key: DB_NAME
        fromDatabase:
          name: jarvis-db
          property: database
      - key: DB_USER
        fromDatabase:
          name: jarvis-db
          property: user
      - key: DB_PASSWORD
        fromDatabase:
          name: jarvis-db
          property: password
      - key: LLM_API_KEY
        sync: false
      - key: LLM_MODEL
        value: gpt-4o-mini

databases:
  - name: jarvis-db
    plan: free
    databaseName: jarvis
    user: jarvis
